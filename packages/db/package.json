{
  "name": "@grabpic/db",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit",
    "migrate": "tsc --module commonjs --moduleResolution node --outDir dist && node dist/migrate.js"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "typescript": "^5.4.0"
  }
}
